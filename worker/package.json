{
  "name": "swimfrancisco",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "type": "module",
  "description": "Swim Francisco conditions Worker — hourly cron fetches NOAA/NDBC, serves /api/conditions.",
  "scripts": {
    "generate-spots": "node ../scripts/generate-worker-spots.mjs",
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "pretypecheck": "node ../scripts/generate-worker-spots.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.83.0"
  }
}
